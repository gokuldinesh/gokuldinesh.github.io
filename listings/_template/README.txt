Copy this folder to public/listings/{slug}/ and replace assets.

Expected layout:
  logo.png              — nav wordmark (optional if no logo in registry)
  og-preview.svg        — Open Graph preview (or .png)
  photos/               — gallery and hero images
    placeholder.png     — default until real photos are added

Registry assets.publicBase must be /listings/{slug} (matches content/rental-listings/{slug}/slug.ts).
